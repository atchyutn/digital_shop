.container  
  %h1
    Orders
  .row
    - current_user.purchases.each do |purchase|
      .col-md-12
        #{ purchase.placed_at }
        Rs #{ purchase.price }
        %a{href: "/orders/#{purchase.id}"} View Order
